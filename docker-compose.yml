services:
  highlight:
    build: .
    image: object-highlight-nl:latest
    ports:
      - "7860:7860"
    volumes:
      - models:/app/model
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

volumes:
  models: