services:
  highlight:
    build: .
    image: object-highlight-nl:latest
    volumes:
      - ./data:/app/data
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]